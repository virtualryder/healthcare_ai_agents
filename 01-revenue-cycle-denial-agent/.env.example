# ── Agent 01 — Revenue-Cycle & Denial ────────────────────────────────────────
EXTRACT_MODE=demo                 # demo (no LLM) | live
CONNECTOR_MODE=fixture            # fixture | live
<<<<<<< HEAD
LLM_PROVIDER=bedrock              # bedrock (default, in-account/PrivateLink under BAA) | anthropic (external API — needs ALLOW_EXTERNAL_LLM=1)
=======
LLM_PROVIDER=anthropic            # anthropic | bedrock
# --- Container auth (secure by default; the shipped image sets both to 1) ---
# AUTH_REQUIRE_JWT=1               # require a verified JWT; serve.py + gateway ignore
#                                  #   body-supplied acting_user_claims (fail closed / 401).
#                                  #   Set to 0 ONLY for the local demo (no authorizer).
# AUTH_REQUIRE_BOUND_APPROVAL=1    # only a bound single-use SoD approval token authorizes
#                                  #   a high-risk write; a bare {approved,reviewer} is refused.
# AUTH_JWKS={"keys":[...]}         # verifier JWKS (inline JSON) — required when AUTH_REQUIRE_JWT=1
# AUTH_JWKS_PATH=/etc/hpp/jwks.json# ...or a file path to the JWKS (alternative to AUTH_JWKS)
# AUTH_JWT_ISSUER=https://cognito-idp.<region>.amazonaws.com/<pool>   # optional iss check
# AUTH_JWT_AUDIENCE=<app-client-id>                                   # optional aud check
>>>>>>> fix(container): enforce JWT + bound approval by default in the Agent-01 image
# ANTHROPIC_API_KEY=...            # required only for LLM_PROVIDER=anthropic
# --- Bedrock (via PrivateLink; no PHI egress to external AI APIs; under AWS BAA) ---
# BEDROCK_REGION=us-east-1
# BEDROCK_GUARDRAIL_ID=...         # REQUIRED in production
# BEDROCK_GUARDRAIL_VERSION=DRAFT
# ENVIRONMENT=production           # makes the guardrail mandatory
# --- Live connectors (point at the org systems) ---
# PAS_BASE_URL=https://pas.health.example.org
# CLEARINGHOUSE_BASE_URL=https://clearinghouse.example.com
# PAYER_BASE_URL=https://payer.example.com/fhir
# EHR_BASE_URL=https://ehr.health.example.org/fhir
# --- Gateway audit sink ---
# GATEWAY_AUDIT_JSONL=/var/log/hpp/gateway-audit.jsonl
