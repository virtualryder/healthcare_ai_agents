"""
hpp_agent_platform — shared, governed platform for the HPP AI agent suite
(Health Providers & Plans).

The agents are not the product. This package — the deny-by-default MCP
authorization gateway, the PHI masker (HIPAA Safe Harbor identifiers), the
provider-abstracted LLM factory with Bedrock Guardrails, the connector framework
(EHR/FHIR, clearinghouse, payer, patient accounting, ...), and the A2A
supervisor — is what makes the agents deployable, auditable, and HIPAA-defensible
in a provider or health-plan environment.
"""
__version__ = "0.1.0"
